# GitHub Pages — project site (repository served at https://<user>.github.io/<repo>/)
# Copy to `.env.production` and set to your repository path with leading and trailing slashes.
# For a user/organization site (username.github.io) leave unset or use `/`
#
# VITE_BASE_PATH=/your-repo-name/
